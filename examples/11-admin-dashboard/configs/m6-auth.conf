# m6-auth config for example 11
private_key = "keys/auth.pem"
public_key  = "keys/auth.pub"
socket      = "/tmp/m6/m6-auth.sock"
db_path     = "data/users.db"

[[user]]
username = "admin"
password = "changeme"
roles    = ["admin"]
